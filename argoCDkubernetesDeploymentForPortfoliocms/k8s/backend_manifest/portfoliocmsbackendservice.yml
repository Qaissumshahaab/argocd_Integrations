apiVersion: v1
kind: Service

metadata:
  name: portfoliocms-backend-service
    

spec:
  type: ClusterIP

  selector:
    app: portfoliocmsbackend

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001
